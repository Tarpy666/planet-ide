{
  "name": "@counted/planet-ide",
  "version": "0.1.0",
  "description": "Browser IDE kernel: file tree, tab lifecycle, command palette, terminal buffer with ANSI parsing.",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "license": "MIT"
}